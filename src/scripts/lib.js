import { typewriter, scrollToSection, fullAge, scrollToBottom, dynamicNavbarColor } from "./util";

export{
    typewriter,
    scrollToSection,
    fullAge,
    scrollToBottom,
    dynamicNavbarColor
}