import { typewriter, scrollToSection, fullAge, scrollToBottom, dynamicNavbarColor, openAside, accordionFunc, steadyCounter } from "./util.js";

export{
    typewriter,
    scrollToSection,
    fullAge,
    scrollToBottom,
    dynamicNavbarColor,
    openAside,
    accordionFunc,
    steadyCounter
}