import { typewriter, scrollToSection, fullAge, scrollToBottom, dynamicNavbarColor } from "./util.js";

export{
    typewriter,
    scrollToSection,
    fullAge,
    scrollToBottom,
    dynamicNavbarColor,
}